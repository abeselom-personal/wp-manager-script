# Copy to .env for local runs: cp .env.example .env
# NOTE: main.py does NOT automatically load .env.
# For an interactive shell you can do:
#   set -a; source .env; set +a
# For production prefer systemd EnvironmentFile or cron.

# IMPORTANT:
# - WPCHECK_KUMA_URL must be the base URL (no UI paths like /dashboard).
# - If your password contains '#', quote it when sourcing with bash:
#     WPCHECK_KUMA_PASS='p@ss#with#hash'

# --- Required ---
WPCHECK_KUMA_URL=http://127.0.0.1:3001
WPCHECK_KUMA_USER=admin
WPCHECK_KUMA_PASS=

# --- Optional (2FA) ---
# WPCHECK_KUMA_2FA_TOKEN=

# --- WordPress / filesystem ---
# WPCHECK_WPCLI=/usr/local/bin/wp
# WPCHECK_HOME_DIR=/home
# WPCHECK_CACHE_FILE=/wpcheck/kuma_cache.json
# WPCHECK_IGNORE_FILE=/wpcheck/wp_checksum_ignore.txt
# WPCHECK_QUARANTINE_DIR=/wpcheck/quarantine

# --- Kuma grouping (optional) ---
# WPCHECK_HTTP_GROUP_ID=
# WPCHECK_CHECKSUM_GROUP_ID=

# --- Intervals / retries ---
# WPCHECK_PUSH_INTERVAL=3600
# WPCHECK_HTTP_INTERVAL=60
# WPCHECK_HTTP_RETRIES=5

# --- Timeouts ---
# WPCHECK_REQUEST_TIMEOUT=15
# WPCHECK_MONITOR_LIST_TIMEOUT=60
# WPCHECK_WP_TIMEOUT=120

# --- Concurrency ---
# WPCHECK_WORKERS=8

# --- Behavior flags ---
# WPCHECK_DRY_RUN=false
# WPCHECK_ENABLE_CORE_REDOWNLOAD=false
# WPCHECK_QUARANTINE_SHOULD_NOT_EXIST=true

# --- Ownership / cleanup ---
# WPCHECK_TAKE_OWNERSHIP=false
# WPCHECK_DELETE_MANAGED_DUPLICATES=true
# WPCHECK_CLEANUP_IGNORED=false
